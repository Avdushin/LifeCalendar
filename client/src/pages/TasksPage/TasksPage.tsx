import { Container, Typography } from '@mui/material';

export const TasksPage = () => {
  return (
    <Container>
      <Typography variant="h4">Задачи</Typography>
      <Typography>Здесь будут задачи пользователя.</Typography>
    </Container>
  );
};
